basePath: /
definitions:
  httpHandler.availableSlotsResponse:
    properties:
      available_slots:
        items:
          $ref: '#/definitions/httpHandler.slotJSON'
        type: array
    type: object
  httpHandler.bookAppointmentRequest:
    properties:
      dealership_id:
        example: d1e2f3a4-b5c6-7890-abcd-ef1234567890
        type: string
      desired_start_time:
        example: "2025-01-15T09:00:00Z"
        type: string
      services:
        example:
        - oil_change
        - tire_rotation
        items:
          type: string
        minItems: 1
        type: array
      vehicle_id:
        example: a1b2c3d4-e5f6-7890-abcd-ef1234567890
        type: string
    required:
    - dealership_id
    - desired_start_time
    - services
    - vehicle_id
    type: object
  httpHandler.bookAppointmentResponse:
    properties:
      appointment_id:
        example: f1a2b3c4-d5e6-7890-abcd-ef1234567890
        type: string
      duration_minutes:
        example: 75
        type: integer
      message:
        example: Appointment booked successfully
        type: string
    type: object
  httpHandler.errorResponse:
    properties:
      error:
        example: dealership not found
        type: string
    type: object
  httpHandler.slotJSON:
    properties:
      end:
        example: "2025-01-15T10:00:00Z"
        type: string
      start:
        example: "2025-01-15T09:00:00Z"
        type: string
    type: object
host: localhost:8081
info:
  contact: {}
  description: RESTful API for booking vehicle service appointments at dealerships.
  title: Keyloop Appointment Booking API
  version: "1.0"
paths:
  /api/v1/appointments:
    post:
      consumes:
      - application/json
      description: Create a new service appointment at a dealership for the chosen
        time slot
      parameters:
      - description: Appointment details
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/httpHandler.bookAppointmentRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/httpHandler.bookAppointmentResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/httpHandler.errorResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/httpHandler.errorResponse'
        "422":
          description: Unprocessable Entity
          schema:
            $ref: '#/definitions/httpHandler.errorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/httpHandler.errorResponse'
      summary: Book an appointment
      tags:
      - appointments
  /api/v1/appointments/available-slots:
    get:
      description: Returns available 30-minute time slots within 7 days of desired_time
        where a bay and qualified technician are both free
      parameters:
      - description: Dealership UUID
        in: query
        name: dealership_id
        required: true
        type: string
      - description: Vehicle UUID
        in: query
        name: vehicle_id
        type: string
      - description: Customer UUID
        in: query
        name: customer_id
        type: string
      - collectionFormat: multi
        description: Service type identifiers (repeatable)
        in: query
        items:
          type: string
        name: services
        type: array
      - description: Total service duration in minutes
        in: query
        name: total_duration_minutes
        required: true
        type: integer
      - description: Desired start datetime (RFC3339)
        in: query
        name: desired_time
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/httpHandler.availableSlotsResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/httpHandler.errorResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/httpHandler.errorResponse'
        "422":
          description: Unprocessable Entity
          schema:
            $ref: '#/definitions/httpHandler.errorResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/httpHandler.errorResponse'
      summary: List available slots
      tags:
      - appointments
swagger: "2.0"
